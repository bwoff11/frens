#At this point, the only point of this document is to hold ideas for later use.


auto_like_own_posts: true

timeline:
  sort_method: [chronological, smart]

jwt_secret: "supersecret"

# Load JWT Secert from env vars, if not load here

database:
  host: "localhost"
  port: 3306
  user: "root"
  password: "root"
  database: "instagram"
  verbose: true # Connect to db.LogMode in db package